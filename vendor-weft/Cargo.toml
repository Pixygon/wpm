[package]
name = "weft"
version = "0.1.0"
edition = "2021"
description = "Weft — the Thread's native code format: content-addressed, verified, total program graphs (spec: docs/spec/weft-v0.1.md)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Pixygon/Infinite"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

